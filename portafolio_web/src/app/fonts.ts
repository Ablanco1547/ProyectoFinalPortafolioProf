import {Red_Hat_Display} from 'next/font/google';

import {Abel} from 'next/font/google';

export const redHat = Red_Hat_Display({subsets: ['latin'], weight: ['400', '500', '600', '700']});

export const abel = Abel({subsets: ['latin'], weight: ['400']});